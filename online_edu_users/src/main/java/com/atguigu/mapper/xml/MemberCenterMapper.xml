<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.mapper.MemberCenterMapper">

    <select id="queryRegistryNum" resultType="java.lang.Integer">
        select count(*) from member_center mc where DATE(mc.gmt_create)=#{day}
    </select>
</mapper>
